#CRUD with SQLite and FastAPI
A functional example of Create/Remove/Update/Delete operations within a SQLite3 database.

Install SQLite:
https://www.sqlite.org/download.html

Create the db:
python source/database/create_db.py

Run the application:
python run.py